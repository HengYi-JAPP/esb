dependencies {
    compile project(':esb-core')
}

mainClassName = "io.vertx.core.Launcher"
jar.manifest {
    attributes(
            'Main-Verticle': "com.hengyi.japp.esb.auth.AuthVerticle",
    )
}